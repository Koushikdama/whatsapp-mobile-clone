// Data is now loaded from data.json via useChatData hook.
// This file can remain for shared configuration constants if needed.

export const APP_NAME = "WhatsApp Clone";
export const CURRENT_USER_ID = "me"; // Kept for fallback, though AppContext provides it now.
